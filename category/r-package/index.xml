<?xml version="1.0" encoding="utf-8" standalone="yes" ?>
<rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom">
  <channel>
    <title>R package | JH&#39;s Blog</title>
    <link>/category/r-package/</link>
      <atom:link href="/category/r-package/index.xml" rel="self" type="application/rss+xml" />
    <description>R package</description>
    <generator>Source Themes Academic (https://sourcethemes.com/academic/)</generator><language>en-us</language><copyright>Copyright 2020 Jaehyeok Shin. All rights reserved.</copyright><lastBuildDate>Sun, 27 Sep 2020 00:00:00 +0000</lastBuildDate>
    <image>
      <url>/images/icon_huc50b28ae5ce84fcc767e7691e9c0c0ae_20181_512x512_fill_lanczos_center_2.png</url>
      <title>R package</title>
      <link>/category/r-package/</link>
    </image>
    
    <item>
      <title>SGLRT Demo</title>
      <link>/draft/sglrt-demo/</link>
      <pubDate>Sun, 27 Sep 2020 00:00:00 +0000</pubDate>
      <guid>/draft/sglrt-demo/</guid>
      <description>&lt;!-- README.md is generated from README.Rmd. Please edit that file --&gt;
&lt;h1 id=&#34;sglrt&#34;&gt;SGLRT&lt;/h1&gt;
&lt;!-- badges: start --&gt;
&lt;!-- badges: end --&gt;
&lt;p&gt;SGLRT is a R package implementation of Sequential Generalized Likelihood Ratio (GLR)-like Tests and confidence seqeunces in &lt;strong&gt;Paper&lt;/strong&gt;.&lt;/p&gt;
&lt;h2 id=&#34;installation&#34;&gt;Installation&lt;/h2&gt;
&lt;p&gt;Currently, the package is under-construction and you can install the development version from 
&lt;a href=&#34;https://github.com/&#34; target=&#34;_blank&#34; rel=&#34;noopener&#34;&gt;GitHub&lt;/a&gt; with:&lt;/p&gt;
&lt;pre&gt;&lt;code class=&#34;language-r&#34;&gt;# install.packages(&amp;quot;devtools&amp;quot;)
devtools::install_github(&amp;quot;shinjaehyeok/SGLRT_public&amp;quot;)
&lt;/code&gt;&lt;/pre&gt;
&lt;!---You can install the released version of SGLRT from [CRAN](https://CRAN.R-project.org) with:

``` r
install.packages(&#34;SGLRT&#34;)
```
--&gt;
&lt;h2 id=&#34;example&#34;&gt;Example&lt;/h2&gt;
&lt;p&gt;Let &lt;code&gt;\(X_1, X_2, \dots\)&lt;/code&gt; be independent sub Gaussian random variables with para. From the Chernoff-Hoeffiding theorem, we have following inequalities:
$$
\mathbb{P}_\mu\left(2n\left(\bar{X}&lt;em&gt;n - \mu\right)^2 \geq \log(2/\alpha)\right) \leq \alpha ~~(\text{sub-Gaussian type}) \&lt;br&gt;
\mathbb{P}&lt;/em&gt;\mu\left(n\mathrm{KL}\left(\bar{X}_n, \mu\right) \geq \log(2/\alpha)\right) \leq \alpha ~~(\text{sub-Bernulli type}),
$$
for any fixed &lt;code&gt;\(n \in \mathbb{N}\)&lt;/code&gt; and &lt;code&gt;\(\alpha \in (0, 1)\)&lt;/code&gt; where &lt;code&gt;\(\mathrm{KL}(\mu_1, \mu_0) := \mu_1 \log\left(\frac{\mu_1}{\mu_0}\right) + (1-\mu_1) \log\left(\frac{1-\mu_1}{1-\mu_0}\right)\)&lt;/code&gt;.&lt;/p&gt;
&lt;p&gt;These inequalities can be converted to confidence intervals &lt;code&gt;\(\{\mathrm{CI}_n(\alpha)\}_{n \in \mathbb{N}}\)&lt;/code&gt; which capture the underlying mean &lt;code&gt;\(\mu\)&lt;/code&gt; with at least &lt;code&gt;\(1-\alpha\)&lt;/code&gt; probability:
$$
\mathbb{P}_\mu \left( \mu \in \mathrm{CI}_n(\alpha)\right) \geq 1-\alpha,~~\forall \mu \in (0,1),
$$
for any &lt;em&gt;fixed&lt;/em&gt; &lt;code&gt;\(n \in \mathbb{N}\)&lt;/code&gt;.&lt;/p&gt;
&lt;p&gt;In practice, it could be difficult to pick a proper sample size &lt;code&gt;\(n\)&lt;/code&gt; before observing the data or we may want to keep tracking the sequence of confidence intervals for some practical reasons. In this case, however, confidence intervals based on the above Chernoff-Hoeffding inequalities do not have the correct coverage. In fact, from the law of iterated logarithm, we can show
$$
\mathbb{P}_\mu \left( \mu \in \mathrm{CI}&lt;em&gt;n(\alpha), \forall n \in \mathbb{N}\right) = 0,~~\forall \mu \in (0,1).
$$
If a sequence of random intervals &lt;code&gt;\(\{\mathrm{CS}_n(\alpha)\}_{n \in \mathbb{N}}\)&lt;/code&gt; satisfies the following inequality:
$$
\mathbb{P}&lt;/em&gt;\mu \left( \mu \in \mathrm{CS}_n(\alpha), \forall n \in \mathbb{N}\right) \geq 1-\alpha,~~\forall \mu \in (0,1),
$$
it is called a  &lt;em&gt;confidence sequence&lt;/em&gt; with confidence level &lt;code&gt;\(\alpha\)&lt;/code&gt; (e.g, see 
&lt;a href=&#34;https://arxiv.org/abs/1810.08240&#34; target=&#34;_blank&#34; rel=&#34;noopener&#34;&gt;Howard et al., 2018&lt;/a&gt; and references therein for a comprehensive summary of related literature.)&lt;/p&gt;
&lt;p&gt;The main objective of &lt;code&gt;SGLRT&lt;/code&gt; package is to implement &lt;em&gt;GLR-like&lt;/em&gt; confidence sequences &lt;code&gt;\(\{\mathrm{CS}_n(\alpha)\}_{n \in \mathbb{N}}\)&lt;/code&gt; in &lt;strong&gt;ADD_CITE&lt;/strong&gt; which has the following features:&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;
&lt;p&gt;For given level &lt;code&gt;\(\alpha \in (0, 0.5)\)&lt;/code&gt; and target interval &lt;code&gt;\([n_{\min}, n_{\max}]\)&lt;/code&gt;, we can compute a threshold &lt;code&gt;\(g_\alpha &amp;gt;0\)&lt;/code&gt; such that the following inequality holds:
$$
\mathbb{P}&lt;em&gt;\mu\left(\exists n \in [n&lt;/em&gt;{\min}, n_{\max}] : 2n\left(\bar{X}_n - \mu\right)^2 \geq g_\alpha  \right) \leq \alpha ~~(\text{sub-Gaussian type}) \&lt;br&gt;
\mathbb{P}_\mu\left(\exists n \in [n_{\min}, n_{\max}] : n\mathrm{KL}\left(\bar{X}_n, \mu\right) \geq g_\alpha\right) \leq \alpha ~~(\text{sub-Bernulli type}),
$$
Of course, we can use the naive threshod &lt;code&gt;\(\log(2 / \alpha) + \log(n_{\max} - n_{\min})\)&lt;/code&gt; based on the union bound. However, the threshold &lt;code&gt;\(g_\alpha\)&lt;/code&gt; we compute has an exponentailly smaller order: &lt;code&gt;\(g_\alpha = O(\log(2 / \alpha) + \log\log(n_{\max} / n_{\min}))\)&lt;/code&gt;.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;Outside of the target interval &lt;code&gt;\([n_{\min}, n_{\max}]\)&lt;/code&gt;, the confidence sequence is well-defined and maintain the valid coverage. That is, we have &lt;code&gt;\(\mathbb{P}_\mu \left( \mu \in \mathrm{CS}_n(\alpha), \forall n \in \mathbb{N}\right) \geq 1-\alpha,~~\forall \mu \in (0,1)\)&lt;/code&gt;.&lt;/p&gt;
&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;We say a confidence sequence &lt;code&gt;\(\{\mathrm{CS}_n(\alpha)\}_{n \in \mathbb{N}}\)&lt;/code&gt; is time-uniformly close to the Chernoff bound on a finite time interval if it satisfies the first feature above. Based on this property, we can use any existing experimentation system which computes the ordinary pointwise valid Chernoff bound or normal confidence bound to calculate a time-uniform confidence sequnece on the target time interval.&lt;/p&gt;
&lt;p&gt;The &lt;code&gt;SGLRT&lt;/code&gt; package also implements the &lt;em&gt;discrete mixture&lt;/em&gt; confidence sequence Though several tyeps of &lt;em&gt;discrete mixture&lt;/em&gt; based confidence sequence have been wel-known in literature, the novel contribution of &lt;code&gt;SGLRT&lt;/code&gt; package is to provide a systemic way of design &lt;em&gt;discrete mixture&lt;/em&gt; confidence seqeunce which is time-uniformly than the &lt;em&gt;GLR-like&lt;/em&gt; one above.&lt;/p&gt;
&lt;h2 id=&#34;demo&#34;&gt;Demo&lt;/h2&gt;
&lt;p&gt;To demonstrate how to use &lt;code&gt;SGLRT&lt;/code&gt; package to compute a time-uniformly valid confidence sequence, let assume we have a stream of independent samples from the Bernoulli distribution with &lt;code&gt;\(\mathbb{P}(X = 1) =  0.5\)&lt;/code&gt;.&lt;/p&gt;
&lt;pre&gt;&lt;code class=&#34;language-r&#34;&gt;set.seed(100)
# Load package
library(SGLRT)

# Generate 1e+4 samples from a Bernoulli(p) with p = 0.5
n &amp;lt;- 1e+4
mu &amp;lt;- 0.5 

n_vec &amp;lt;- seq(1, n)
x_vec &amp;lt;- rbinom(n, 1, mu) 
x_bar_vec &amp;lt;- cumsum(x_vec) / n_vec

# Set alpha = 0.025 for 95% CI.
alpha &amp;lt;- 0.025

# Set target time interval: [1, 1e+4] and [100, 1e+4]
nmin1 &amp;lt;- 1
nmax1 &amp;lt;- 1e+4
nmin2 &amp;lt;- 2e+2
nmax2 &amp;lt;- 1e+4
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Now, let us first compare the poinwise and time-uniform Chernoff bounds in the sub-Gaussian type.&lt;/p&gt;
&lt;pre&gt;&lt;code class=&#34;language-r&#34;&gt;# Compute the pointwise Chernoff bound.
point_G_bound &amp;lt;- sqrt(log(2/alpha) / (2 * n_vec))

# Compute the time-uniform GLR-like bound.
G_out1 &amp;lt;- SGLR_CI_additive(alpha, nmax1, nmin1)
GLR_like_G_bound1 &amp;lt;- sapply(n_vec, G_out1$GLR_like_fn)

G_out2 &amp;lt;- SGLR_CI_additive(alpha, nmax2, nmin2)
GLR_like_G_bound2 &amp;lt;- sapply(n_vec, G_out2$GLR_like_fn)

# Compute the time-uniform discrete mixture bound.
dis_mix_G_bound1 &amp;lt;- sapply(n_vec, G_out1$dis_mix_fn)
dis_mix_G_bound2 &amp;lt;- sapply(n_vec, G_out2$dis_mix_fn)
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;&lt;img src=&#34;man/figures/README-unnamed-chunk-2-1.png&#34; width=&#34;100%&#34; /&gt;&lt;/p&gt;
</description>
    </item>
    
    <item>
      <title>SGLRT package </title>
      <link>/post/sglrt-package/</link>
      <pubDate>Sun, 27 Sep 2020 00:00:00 +0000</pubDate>
      <guid>/post/sglrt-package/</guid>
      <description>


&lt;div id=&#34;sglrt&#34; class=&#34;section level1&#34;&gt;
&lt;h1&gt;SGLRT&lt;/h1&gt;
&lt;p&gt;&lt;code&gt;SGLRT&lt;/code&gt; is a R package implementation of Sequential Generalized Likelihood Ratio (GLR)-like Tests and confidence sequences in &lt;strong&gt;Paper&lt;/strong&gt;.&lt;/p&gt;
&lt;div id=&#34;installation&#34; class=&#34;section level2&#34;&gt;
&lt;h2&gt;Installation&lt;/h2&gt;
&lt;p&gt;You can install the &lt;code&gt;SGLRT&lt;/code&gt; package from &lt;a href=&#34;https://github.com/&#34;&gt;GitHub&lt;/a&gt; with:&lt;/p&gt;
&lt;pre class=&#34;r&#34;&gt;&lt;code&gt;# install.packages(&amp;quot;devtools&amp;quot;)
devtools::install_github(&amp;quot;shinjaehyeok/SGLRT_public&amp;quot;)&lt;/code&gt;&lt;/pre&gt;
&lt;!---You can install the released version of SGLRT from [CRAN](https://CRAN.R-project.org) with:

``` r
install.packages(&#34;SGLRT&#34;)
```
--&gt;
&lt;/div&gt;
&lt;div id=&#34;how-to-reproduce-all-plots-and-simulation-results-in-paper.&#34; class=&#34;section level2&#34;&gt;
&lt;h2&gt;How to reproduce all plots and simulation results in &lt;strong&gt;paper&lt;/strong&gt;.&lt;/h2&gt;
&lt;p&gt;To reproduce plots, you need to install &lt;code&gt;latex2exp&lt;/code&gt; package which parses and converts LaTeX math formulas to R’s plotmath expressions. It is is not installed, you can run the following command to install is.&lt;/p&gt;
&lt;pre class=&#34;r&#34;&gt;&lt;code&gt;install.packages(&amp;quot;latex2exp&amp;quot;)&lt;/code&gt;&lt;/pre&gt;
&lt;div id=&#34;compare-boundaries-for-sequential-glr-tests.-fig.-3-in-section-iii-a&#34; class=&#34;section level3&#34;&gt;
&lt;h3&gt;1. Compare boundaries for sequential GLR tests. &lt;em&gt;(Fig. 3 in Section III-A)&lt;/em&gt;&lt;/h3&gt;
&lt;p&gt;For a given exponential family distribution, let &lt;span class=&#34;math inline&#34;&gt;\(\mathrm{GLR}_n (&amp;gt; \mu_1, \leq\mu_0)\)&lt;/span&gt; be the GLR statistic based &lt;span class=&#34;math inline&#34;&gt;\(n\)&lt;/span&gt; i.i.d. observations for the one-sided testing problem:
&lt;span class=&#34;math display&#34;&gt;\[
H_0 : \mu \leq \mu_0~~\text{vs}~~H_1 : \mu &amp;gt;\mu_1,
\]&lt;/span&gt;
for some &lt;span class=&#34;math inline&#34;&gt;\(\mu_0 &amp;lt; \mu_0\)&lt;/span&gt; in the space of mean parameters. For any constant &lt;span class=&#34;math inline&#34;&gt;\(g &amp;gt; 0\)&lt;/span&gt;, Lorden 1973 proved the following upper bound on the boundary-crossing probability:&lt;sup&gt;&lt;a href=&#34;#myfootnote1&#34;&gt;1&lt;/a&gt;&lt;/sup&gt;
&lt;span class=&#34;math display&#34;&gt;\[
\sup_{\mu \leq \mu_0}\mathbb{P}_{\mu} \left(n \geq 1:   \log\mathrm{GLR}_n(&amp;gt;\mu_1,\leq\mu_0) \geq g\right)
\leq \begin{cases} e^{-g} &amp;amp;\mbox{if } \mathrm{KL}(\mu_1,\mu_0) \geq g \\
\left(1 + \frac{g}{\mathrm{KL}(\mu_1,\mu_0)} \right)e^{-g} &amp;amp; \mbox{otherwise }.
\end{cases}
\]&lt;/span&gt;&lt;/p&gt;
&lt;p&gt;Alternatively, in our paper, we proved the following inequality holds:&lt;/p&gt;
&lt;p&gt;&lt;span class=&#34;math display&#34;&gt;\[
\sup_{\mu \leq \mu_0}\mathbb{P}_{\mu} \left(\exists n \geq 1:   \log\mathrm{GLR}_n(&amp;gt;\mu_1,\leq\mu_0) \geq g\right) 
\leq \begin{cases} e^{-g} &amp;amp;\mbox{if } \mathrm{KL}(\mu_1,\mu_0) \geq g \\
\inf_{\eta &amp;gt;1} \left\lceil \log_\eta \left(\frac{g}{\mathrm{KL}(\mu_1,\mu_0)}\right)\right\rceil e^{-g / \eta} &amp;amp; \mbox{otherwise }. 
\end{cases}
\]&lt;/span&gt;
(In the paper, a nonparametric generalization of the above inequality was presented.)&lt;/p&gt;
&lt;p&gt;Now, for any &lt;span class=&#34;math inline&#34;&gt;\(\alpha \in (0,1)\)&lt;/span&gt;, let &lt;span class=&#34;math inline&#34;&gt;\(g_\alpha^L(\mu_1,\mu_0)\)&lt;/span&gt; and &lt;span class=&#34;math inline&#34;&gt;\(g_\alpha(\mu_1,\mu_0)\)&lt;/span&gt; be smallest boundary values which make RHS of two above inequalities equal to &lt;span class=&#34;math inline&#34;&gt;\(\alpha\)&lt;/span&gt;, respectively. The following Rcode reproduces the Fig. 3 in Section III-A in which we compared &lt;span class=&#34;math inline&#34;&gt;\(g_\alpha^L(\mu_1,\mu_0)\)&lt;/span&gt; and &lt;span class=&#34;math inline&#34;&gt;\(g_\alpha(\mu_1,\mu_0)\)&lt;/span&gt; for normal distributions with &lt;span class=&#34;math inline&#34;&gt;\(\sigma = 1\)&lt;/span&gt;. (In this case, &lt;span class=&#34;math inline&#34;&gt;\(\mathrm{KL}(\mu_1 ,\mu_0) = (\mu_1 - \mu_0)^2 / 2\)&lt;/span&gt;.)&lt;/p&gt;
&lt;pre class=&#34;r&#34;&gt;&lt;code&gt;library(latex2exp)
library(SGLRT)


alpha &amp;lt;- 10^seq(-1,-10)
theta_vec &amp;lt;- c(1,0.5, 10^seq(-1,-10)) 
theta_vec &amp;lt;- 2^seq(0,-30)

d_vec &amp;lt;- theta_vec^2 / 2

for (i in seq_along(alpha[1:3])){
  f_lorden &amp;lt;- function(d) const_boundary_lorden(alpha[i], d)
  lorden &amp;lt;- sapply(d_vec, f_lorden)
  f_ours &amp;lt;- function(d) const_boundary(alpha[i], d)
  ours  &amp;lt;- sapply(d_vec, f_ours)
  if (i == 1){
    plot(1/theta_vec, unlist(lorden[&amp;quot;g&amp;quot;,]), type = &amp;quot;l&amp;quot;, 
         log =&amp;quot;x&amp;quot;,
         ylab = &amp;quot;Boundary Value&amp;quot;,
         xlab = TeX(&amp;quot;$|\\mu_1 - \\mu_0|^{-1}$ (log scale)&amp;quot;))
    points(1/theta_vec, unlist(ours[&amp;quot;g&amp;quot;,]), type = &amp;quot;l&amp;quot;, col = 2)
  } else {
    points(1/theta_vec, unlist(lorden[&amp;quot;g&amp;quot;,]), type = &amp;quot;l&amp;quot;, col = 1,
           lty = i)
    points(1/theta_vec, unlist(ours[&amp;quot;g&amp;quot;,]), type = &amp;quot;l&amp;quot;, col = 2,
           lty = i)
  }
}
legend(&amp;quot;topleft&amp;quot;, TeX(c(paste0(&amp;quot;Lorden&amp;#39;s ($\\alpha = &amp;quot;, alpha[1:3],&amp;quot;$)&amp;quot;), paste0(&amp;quot;Ours ($\\alpha = &amp;quot;, alpha[1:3],&amp;quot;$)&amp;quot;))), col = c(rep(1,3), rep(2,3)), lty = rep(1:3,2))&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;&lt;img src=&#34;/post/2020-09-27-sglrt-package.en_files/figure-html/Fig.3-1.png&#34; width=&#34;672&#34; /&gt;&lt;/p&gt;
&lt;/div&gt;
&lt;div id=&#34;ratio-of-cis-width-to-clt-fig.-5-in-section-iv-c&#34; class=&#34;section level3&#34;&gt;
&lt;h3&gt;2. Ratio of CI’s width to CLT &lt;em&gt;(Fig. 5 in Section IV-C)&lt;/em&gt;&lt;/h3&gt;
&lt;p&gt;For each &lt;span class=&#34;math inline&#34;&gt;\(n\)&lt;/span&gt;, let &lt;span class=&#34;math inline&#34;&gt;\(\mathrm{CI}_n(\alpha)\)&lt;/span&gt; be a random set based on first &lt;span class=&#34;math inline&#34;&gt;\(n\)&lt;/span&gt; observations &lt;span class=&#34;math inline&#34;&gt;\(X_1, \dots, X_n\)&lt;/span&gt; from a distribution with mean &lt;span class=&#34;math inline&#34;&gt;\(\mu\)&lt;/span&gt;. If the sequence of random sets &lt;span class=&#34;math inline&#34;&gt;\(\{\mathrm{CI}_n(\alpha)\}_{n \in \mathbb{N}}\)&lt;/span&gt; satisfies the following inequality:
&lt;span class=&#34;math display&#34;&gt;\[
\mathbb{P}_\mu \left( \mu \in \mathrm{CI}_n(\alpha), \forall n \in \mathbb{N}\right) \geq 1-\alpha,~~\forall \mu \in (0,1),
\]&lt;/span&gt;
it is called a &lt;em&gt;confidence sequence&lt;/em&gt; with confidence level &lt;span class=&#34;math inline&#34;&gt;\(\alpha\)&lt;/span&gt; (e.g, see &lt;a href=&#34;https://arxiv.org/abs/1810.08240&#34;&gt;Howard et al., 2018&lt;/a&gt;&lt;sup&gt;&lt;a href=&#34;#myfootnote2&#34;&gt;2&lt;/a&gt;&lt;/sup&gt; and references therein for a comprehensive summary of related literature.)&lt;/p&gt;
&lt;p&gt;Based on the upper bound on the boundary crossing probability above, in our paper, we presented two novel confidence sequences called &lt;em&gt;GLR-like&lt;/em&gt; and &lt;em&gt;discrete mixture&lt;/em&gt; based confidence sequences. For sub-Gaussian distributions with parameter &lt;span class=&#34;math inline&#34;&gt;\(\sigma\)&lt;/span&gt;, The GLR-like confidence sequence is given by
&lt;span class=&#34;math display&#34;&gt;\[
    \mathrm{CI}_n^{\mathrm{G}} := \begin{cases}  \left(\bar{X}_n - \sigma\sqrt{\frac{g_\alpha}{n_{\min}}}\left[  \sqrt{2}  + \frac{1}{\sqrt{2}}\left(\frac{n_{\min}}{n}-1\right)\right], \infty\right) &amp;amp;\mbox{if } n \in [n_0,n_{\min}) \\
 \left( \bar{X}_n - \sigma\sqrt{\frac{2g_\alpha}{n}}, \infty\right) &amp;amp;\mbox{if }  n \in [n_{\min}, n_{\max}] \\
 \left(\bar{X}_n - \sigma\sqrt{\frac{g_\alpha}{n_{\max}}}\left[ \sqrt{2}  + \frac{1}{\sqrt{2}}\left(\frac{n_{\max}}{n}-1\right)\right], \infty\right) &amp;amp;\mbox{if } n \in (n_{\max}, \infty)
  \end{cases},
\]&lt;/span&gt;
for any given level &lt;span class=&#34;math inline&#34;&gt;\(\alpha \in (0, 1)\)&lt;/span&gt; and target time interval &lt;span class=&#34;math inline&#34;&gt;\([n_{\min}, n_{\max}]\)&lt;/span&gt; on which the confidence sequence is time-uniformly close to the Chrenoff bound. Unlike the GLR-like one, the discrete mixture based confidence sequence &lt;span class=&#34;math inline&#34;&gt;\(\{\mathrm{CI}_n^{\mathrm{DM}}\}\)&lt;/span&gt; does not have an explicit form and see the paper for the detailed explanation how to compute &lt;span class=&#34;math inline&#34;&gt;\(\mathrm{CI}_n^{\mathrm{DM}}\)&lt;/span&gt; for given sample mean &lt;span class=&#34;math inline&#34;&gt;\(\bar{X}_n\)&lt;/span&gt; and confidence level &lt;span class=&#34;math inline&#34;&gt;\(\alpha\)&lt;/span&gt;. By the construction, we have &lt;span class=&#34;math inline&#34;&gt;\(\mathrm{CI}_n^{\mathrm{G}} \subset \mathrm{CI}_n^{\mathrm{DM}}\)&lt;/span&gt;.&lt;/p&gt;
&lt;p&gt;In Section IV-C of the paper, we compare these bounds with the stitching and normal mixture bounds in &lt;a href=&#34;https://arxiv.org/abs/1810.08240&#34;&gt;Howard et al., 2018&lt;/a&gt;&lt;sup&gt;&lt;a href=&#34;#myfootnote2&#34;&gt;2&lt;/a&gt;&lt;/sup&gt; where each confidence intervals for stitching &lt;span class=&#34;math inline&#34;&gt;\(\mathrm{CI}_{n}^{\mathrm{ST}}\)&lt;/span&gt; and normal mixture method &lt;span class=&#34;math inline&#34;&gt;\(\mathrm{CI}_n^{\mathrm{NM}}\)&lt;/span&gt; is given by
&lt;span class=&#34;math display&#34;&gt;\[
\begin{aligned}
    \mathrm{CI}_{n}^{\mathrm{ST}} &amp;amp;:= \left(\bar{X}_n-  \frac{1.7}{\sqrt{n}}\sqrt{\log\log(2n) + 0.72 \log\left(\frac{5.2}{\alpha}\right)}, \infty \right) \\
    \mathrm{CI}_{n}^{\mathrm{NM}} &amp;amp;:= \left(\bar{X}_n-  \sqrt{2 \left(1 + \frac{\rho}{n}\right) \log\left(\frac{1}{2\alpha} \sqrt{\frac{n + \rho}{\rho} + 1}\right)}, \infty \right),
\end{aligned}
\]&lt;/span&gt;
where we set &lt;span class=&#34;math inline&#34;&gt;\(\rho = 1260\)&lt;/span&gt; by following the setting in Figure 9 of &lt;a href=&#34;https://arxiv.org/abs/1810.08240&#34;&gt;Howard et al., 2018&lt;/a&gt;&lt;sup&gt;&lt;a href=&#34;#myfootnote2&#34;&gt;2&lt;/a&gt;&lt;/sup&gt;.&lt;/p&gt;
&lt;p&gt;The following Rcode reproduces the Fig. 5 in Section IV-C in which we compared ratios of widths of confidence intervals above to the pointwise and asymptotically valid normal confidence intervals based on the central limit theorem.&lt;/p&gt;
&lt;pre class=&#34;r&#34;&gt;&lt;code&gt;library(SGLRT)

# Chernoff
chornoff &amp;lt;- function(v, alpha = 0.025){
  sqrt(2  * log(1/alpha) / v)
}

# Normal mixture function
normal_mix &amp;lt;- function(v, alpha = 0.025, rho = 1260){
  sqrt(2 * (1 + rho / v) * log(1/(2*alpha) * sqrt((v + rho)/rho) + 1)) 
}

#Stitching
stitch &amp;lt;- function(v, alpha = 0.025){
  1.7 * sqrt((log(log(2) + log(v)) + 0.72 * log(5.2/alpha)) / v) 
}

# CLT
CLT &amp;lt;- function(v, alpha = 0.025){
  qnorm(1-alpha) / sqrt(v)
}

# Functions to construct our bounds
# First one has target interval [1, nmax]
# Second one has target interval [nmax / 20, nmax * 4]

alpha &amp;lt;- 0.025
nmax &amp;lt;- 1e+5

nmax1 &amp;lt;- nmax
nmin1 = 1

nmax2 &amp;lt;- nmax1 * 4
nmin2 &amp;lt;- nmax1 / 20


ours1 &amp;lt;- SGLR_CI_additive(alpha, nmax1, nmin1)
ours2 &amp;lt;- SGLR_CI_additive(alpha, nmax2, nmin2)


# Compute the width of CIs on exponentially-spaced grid.
M &amp;lt;- log(nmax, base = 10)
v &amp;lt;- 10^(seq(0,M + 0.2, length.out = 100))


# Compute existing bounds
chornoff_vec &amp;lt;- sapply(v, chornoff)
CLT_vec &amp;lt;- sapply(v, CLT)
normal_mix_vec &amp;lt;- sapply(v, normal_mix)
stit_vec &amp;lt;- sapply(v, stitch)

existing_list &amp;lt;- list(stitch = stit_vec,
                      normal_mix = normal_mix_vec)

# Compute our bounds
GLR_like_1_vec &amp;lt;- sapply(v, ours1$GLR_like_fn)
GLR_like_2_vec &amp;lt;- sapply(v, ours2$GLR_like_fn)
our_dis_mix_1_vec &amp;lt;- sapply(v, ours1$dis_mix_fn)
our_dis_mix_2_vec &amp;lt;- sapply(v, ours2$dis_mix_fn)


ours_list_1 &amp;lt;- list(GLR_like_1 = GLR_like_1_vec,
                    our_dis_mix_1 = our_dis_mix_1_vec)

ours_list_2 &amp;lt;- list(GLR_like_2 = GLR_like_2_vec,
                    our_dis_mix_2 = our_dis_mix_2_vec)

# Plot ratio of bounds
title &amp;lt;- &amp;quot;Ratio of CI&amp;#39;s width to CLT&amp;quot;
plot(v, chornoff_vec / CLT_vec, type = &amp;quot;l&amp;quot;,
     main = title,
     ylab = &amp;quot;Ratio&amp;quot;,
     xlab = &amp;quot;n&amp;quot;,
     ylim = c(1, 4),
     xlim = c(1, nmax))
col = 1
legend_col &amp;lt;- c(1)
for (i in seq_along(existing_list)){
  col &amp;lt;- col + 1
  legend_col &amp;lt;- c(legend_col, col)
  lines(v, existing_list[[i]] / CLT_vec, col = col)
}
legend_lty &amp;lt;- rep(1, length(existing_list) + 1)
for (i in seq_along(ours_list_1)){
  col &amp;lt;- col + 1
  legend_col &amp;lt;- c(legend_col, col)
  lines(v, ours_list_1[[i]] / CLT_vec,
        lty = 2, lwd = 2, col = col)
}
legend_lty &amp;lt;- c(legend_lty, rep(2, length(ours_list_1)))
for (i in seq_along(ours_list_2)){
  col &amp;lt;- col + 1
  legend_col &amp;lt;- c(legend_col, col)
  lines(v, ours_list_2[[i]] / CLT_vec,
        lty = 4, lwd = 2, col = col)
}
legend_lty &amp;lt;- c(legend_lty, rep(4, length(ours_list_2)))
abline(v = c(nmin1, nmin2, nmax1, nmax2), lty = 3)
bounds_name &amp;lt;- c(&amp;quot;Chernoff&amp;quot;, &amp;quot;Stitching (HRMS&amp;#39;20)&amp;quot;, &amp;quot;Normal Mix. (HRMS&amp;#39;20)&amp;quot;,
                 &amp;quot;GLR-like 1 (Ours)&amp;quot;, &amp;quot;Discrete Mix. 1 (Ours)&amp;quot;,
                 &amp;quot;GLR-like 2 (Ours)&amp;quot;, &amp;quot;Discrete Mix. 2 (Ours)&amp;quot;)
legend(&amp;quot;topright&amp;quot;, bounds_name,
       lty = legend_lty,
       col = legend_col,
       bg= &amp;quot;white&amp;quot;)&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;&lt;img src=&#34;/post/2020-09-27-sglrt-package.en_files/figure-html/Fig.5-1.png&#34; width=&#34;672&#34; /&gt;&lt;/p&gt;
&lt;/div&gt;
&lt;/div&gt;
&lt;div id=&#34;reference&#34; class=&#34;section level2&#34;&gt;
&lt;h2&gt;Reference&lt;/h2&gt;
&lt;p&gt;&lt;a name=&#34;myfootnote1&#34;&gt;1&lt;/a&gt;: G. Lorden, “Open-ended tests for Koopman-Darmois families,”The Annals of Statistics, vol. 1, no. 4, pp. 633–643, 1973.&lt;/p&gt;
&lt;p&gt;&lt;a name=&#34;myfootnote2&#34;&gt;2&lt;/a&gt;: S. R. Howard, A. Ramdas, J. McAuliffe, and J. Sekhon, “Uniform, nonparametric, non-asymptotic confidence sequences,”arXiv preprint arXiv:1810.08240, 2018.&lt;/p&gt;
&lt;/div&gt;
&lt;/div&gt;
</description>
    </item>
    
  </channel>
</rss>
